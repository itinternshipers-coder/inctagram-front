# 📋 Git Workflow для команды

## Ежедневный workflow:

### 1. `git fetch --all` - получить свежие изменения
### 2. `git checkout ваша-ветка` - перейти в свою ветку
### 3. Обновиться одним из способов ↓
### 4. Работать, коммитить
### 5. `git push --force-with-lease`

# Способы обновления ветки:

## 🔄 REBASE (рекомендуется)

```bash
# Полный процесс:
git fetch origin
git rebase origin/develop
```

### Если конфликты:

### 1. Редактируем файлы с конфликтами

### 2. git add .

### 3. git rebase --continue

### 4. git push --force-with-lease

## 🔀 MERGE (альтернатива)

```bash
# Полный процесс:
git fetch origin
git merge origin/develop
```

## Если конфликты:

### 1. Редактируем файлы с конфликтами

### 2. git add .

### 3. git commit -m "merge: resolve conflicts"

### 4. git push


