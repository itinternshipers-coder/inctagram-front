#!/usr/bin/env sh

echo "🔍 Проверяем актуальность ветки..."

git fetch origin

if git merge-base --is-ancestor origin/develop HEAD; then
  echo "✅ Ветка актуальна относительно develop"
else
  echo "❌ Твоя ветка отстает от develop!"
  echo ""
  echo "Выбери способ обновления:"
  echo "🔄 1. REBASE (рекомендуется)"
  echo "🔀 2. MERGE (проще)"
  echo "📋 3. Я уже обновил ветку вручную"
  echo "После обновления запуши снова: git push --force-with-lease"
  exit 1
fi

echo "🚀 Проверяем линтинг staged файлов..."

# Получаем staged файлы с расширениями для линта
STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep -E "\.(js|jsx|ts|tsx|css|scss)$")

if [ -n "$STAGED_FILES" ]; then
  echo "⚡ Файлы для линта: $STAGED_FILES"

  pnpm exec eslint $STAGED_FILES --fix
  if [ $? -ne 0 ]; then
    echo "❌ ESLint завершился с ошибками!"
    exit 1
  fi

  pnpm exec stylelint $STAGED_FILES --fix
  if [ $? -ne 0 ]; then
    echo "❌ Stylelint завершился с ошибками!"
    exit 1
  fi
else
  echo "ℹ️ Нет staged файлов для линта, пропускаем линтинг."
fi

echo "🚀 Сборка проекта с кэшированием..."
pnpm exec next build --turbo --no-lint --cache .next/cache
if [ $? -ne 0 ]; then
  echo "❌ Сборка завершилась с ошибками!"
  exit 1
fi

echo "✅ Все проверки пройдены! Пушим..."



